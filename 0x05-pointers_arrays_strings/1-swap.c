/**
 *swap_int - counter counter
 *@a: is a pointer
 *@b: is a pointer too I guess
 *Return: probably a nuthin
 */
#include "main.h"
void swap_int(int *a, int *b)
{
int y;

*a = y;
*b = *a;
y = *b;
}
