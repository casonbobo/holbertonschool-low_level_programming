#include "holberton.h"
/**
 * reverse_array - reverses an array
 * @a: aaaaaaaa
 * @n: nnnnnnnn
 * Return: nuthin
 */
void reverse_array(int *a, int n);
{
	int index = 0;

	while (index < n)
		index++;

	for (; index > 0; index--)
		a[] = a[index];
}
