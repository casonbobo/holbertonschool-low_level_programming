#include "main.h"
#include <stdio.h>
/**
 *main - can say it's own name
 *@argc: the machines are taking over
 *@argv: oh no
 *Return: nothing at all
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
}
